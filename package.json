{
  "name": "illuminarchism",
  "version": "1.0.0",
  "description": "Historical Atlas Editor",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp -r src dist/ && cp _headers dist/ && cp -r atlases dist/"
  },
  "keywords": [
    "atlas",
    "history",
    "map"
  ],
  "author": "bentezhko",
  "license": "MIT"
}